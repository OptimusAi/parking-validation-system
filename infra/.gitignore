# Local Docker Compose overrides
docker-compose.override.yml

# Postgres data mounted locally (if changed to bind mount)
postgres/data/

# Any local init scripts with embedded secrets
postgres/init-local.sql
